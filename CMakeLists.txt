####################################################################################################
# CMakeLists file for Atom
# Generated by SwampMonster
# 2025-12-13
####################################################################################################

####################################################################################################
# basic project config
cmake_minimum_required(VERSION 3.12)
set(project_name "Atom")
set(CMAKE_MODULE_PATH ${CMAKE_SOURCE_DIR}/cmake_modules ${CMAKE_MODULE_PATH})
set(CMAKE_CXX_STANDARD 17)

####################################################################################################
# load modules
include(SuperColliderServerPlugin RESULT_VARIABLE server_plugin_found)
if(NOT server_plugin_found)
    message(FATAL_ERROR "Could not find server plugin functions module")
endif()

include(SuperColliderCompilerConfig RESULT_VARIABLE compiler_config_found)
if(NOT compiler_config_found)
    message(FATAL_ERROR "Could not find compiler config module")
endif()

# Windows - puts redistributable DLLs in install directory
include(InstallRequiredSystemLibraries)

sc_check_sc_path("${SC_PATH}")
message(STATUS "Found SuperCollider: ${SC_PATH}")
set(SC_PATH "${SC_PATH}" CACHE PATH
    "Path to SuperCollider source. Relative paths are treated as relative to this script" FORCE)

include("${SC_PATH}/SCVersion.txt")
message(STATUS "Building plugins for SuperCollider version: ${SC_VERSION}")

# set project here to avoid SCVersion.txt clobbering our version info
project(${project_name})
sc_do_initial_compiler_config() # do after setting project so compiler ID is available

if(CMAKE_INSTALL_PREFIX_INITIALIZED_TO_DEFAULT OR NOT CMAKE_INSTALL_PREFIX)
    message(WARNING "No install prefix provided, defaulting to $BUILD_DIR/install")
    set(CMAKE_INSTALL_PREFIX "${CMAKE_BINARY_DIR}/install" CACHE PATH "Install prefix" FORCE)
endif()

message(STATUS "Install directory set to: ${CMAKE_INSTALL_PREFIX}")

####################################################################################################
# options
option(SUPERNOVA "Build plugins for supernova" ON)
option(SCSYNTH "Build plugins for scsynth" ON)
option(NATIVE "Optimize for native architecture" OFF)
option(STRICT "Use strict warning flags" OFF)
option(NOVA_SIMD "Build plugins with nova-simd support." ON)

####################################################################################################
# include libraries

if (NOVA_SIMD)
	add_definitions(-DNOVA_SIMD)
	include_directories(${SC_PATH}/external_libraries/nova-simd)
endif()

####################################################################################################
# Begin target Atom

set(Atom_cpp_files
    plugins/Atom/Atom.cpp
    plugins/Atom/Atom.hpp
)
set(Atom_sc_files
    plugins/Atom/Atom.sc
)
set(Atom_schelp_files
    plugins/Atom/Atom.schelp
    plugins/Atom/AtomSpace.schelp
)

sc_add_server_plugin(
    "plugins/Atom/Atom" # desination directory
    "Atom" # target name
    "${Atom_cpp_files}"
    "${Atom_sc_files}"
    "${Atom_schelp_files}"
)

# End target Atom
####################################################################################################

####################################################################################################
# Begin target Kick
####################################################################################################

set(Kick_cpp_files
    plugins/Kick/Kick.cpp
    plugins/Kick/Kick.hpp
)
set(Kick_sc_files
    plugins/Kick/Kick.sc
)
set(Kick_schelp_files
    plugins/Kick/Kick.schelp
)

sc_add_server_plugin(
    "plugins/Kick/Kick" # destination directory
    "Kick" # target name
    "${Kick_cpp_files}"
    "${Kick_sc_files}"
    "${Kick_schelp_files}"
)

# End target Kick
####################################################################################################

####################################################################################################
# Begin target Bassline
####################################################################################################

set(Bassline_cpp_files
    plugins/Bassline/Bassline.cpp
    plugins/Bassline/Bassline.hpp
)
set(Bassline_sc_files
    plugins/Bassline/Bassline.sc
)
set(Bassline_schelp_files
    plugins/Bassline/Bassline.schelp
)

sc_add_server_plugin(
    "plugins/Bassline/Bassline" # destination directory
    "Bassline" # target name
    "${Bassline_cpp_files}"
    "${Bassline_sc_files}"
    "${Bassline_schelp_files}"
)

# End target Bassline
####################################################################################################

####################################################################################################
# Begin target Womp
####################################################################################################

set(Womp_cpp_files
    plugins/Womp/Womp.cpp
    plugins/Womp/Womp.hpp
)
set(Womp_sc_files
    plugins/Womp/Womp.sc
)
set(Womp_schelp_files
    plugins/Womp/Womp.schelp
)

sc_add_server_plugin(
    "plugins/Womp/Womp" # destination directory
    "Womp" # target name
    "${Womp_cpp_files}"
    "${Womp_sc_files}"
    "${Womp_schelp_files}"
)

# End target Womp
####################################################################################################

####################################################################################################
# Begin target Sylenth
####################################################################################################

# Note: Sylenth is a SuperCollider class wrapper, not a C++ plugin
# It wraps VSTPlugin for easier Sylenth1 usage
set(Sylenth_sc_files
    plugins/Sylenth/Sylenth.sc
)
set(Sylenth_schelp_files
    plugins/Sylenth/Sylenth.schelp
)

# Install SuperCollider class and help files (no C++ plugin to build)
install(FILES ${Sylenth_sc_files} DESTINATION plugins/Sylenth/Sylenth/Classes)
install(FILES ${Sylenth_schelp_files} DESTINATION plugins/Sylenth/Sylenth/HelpSource/Classes)

# End target Sylenth
####################################################################################################

####################################################################################################
# Kick2 - VST Wrapper
####################################################################################################

# Note: Kick2 is a SuperCollider class wrapper, not a C++ plugin
# It wraps VSTPlugin for easier Kick 2 usage
set(Kick2_sc_files
    plugins/Kick2/Kick2.sc
)
set(Kick2_schelp_files
    plugins/Kick2/Kick2.schelp
)

# Install SuperCollider class and help files (no C++ plugin to build)
install(FILES ${Kick2_sc_files} DESTINATION plugins/Kick2/Kick2/Classes)
install(FILES ${Kick2_schelp_files} DESTINATION plugins/Kick2/Kick2/HelpSource/Classes)

# End target Kick2
####################################################################################################

####################################################################################################
# END PLUGIN TARGET DEFINITION
####################################################################################################

message(STATUS "Generating plugin targets done")
